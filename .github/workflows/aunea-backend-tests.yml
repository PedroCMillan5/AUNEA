name: AUNEA Backend Tests

on:
  push:
    paths:
      - '3_SYSTEM/backend/**'
      - '.github/workflows/aunea-backend-tests.yml'
  pull_request:
    paths:
      - '3_SYSTEM/backend/**'
      - '.github/workflows/aunea-backend-tests.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 3_SYSTEM/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install '.[test]'
      - run: pytest -q
