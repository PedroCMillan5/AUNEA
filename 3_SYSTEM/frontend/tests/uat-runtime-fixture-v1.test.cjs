const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const src=fs.readFileSync(path.join(root,'app-uat-fixtures-v1.js'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');test('one-click UAT creates identifiable isolated runtime records without operational inserts',()=>{for(const id of ['UAT-CMP-001','UAT-CON-001','UAT-ENG-001','UAT-STEP-001','UAT-FRI-001'])assert.match(src,new RegExp(id));assert.match(src,/operational_collections_unchanged/);assert.doesNotMatch(src,/state\.companies\.push|state\.contacts\.push|state\.engagements\.push|state\.projects\.push/);assert.match(html,/app-uat-fixtures-v1\.js/);});
