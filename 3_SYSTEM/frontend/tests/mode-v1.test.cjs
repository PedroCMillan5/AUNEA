const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');test('B6 exposes shared-state Session/Internal modes',()=>{const src=fs.readFileSync(path.join(root,'app-mode-v1.js'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');assert.match(src,/SESSION_PAGES/);assert.match(src,/Modo Sesión/);assert.match(src,/Modo Interno/);assert.match(src,/state\.uiMode/);assert.match(src,/mode-session/);assert.match(html,/app-mode-v1\.js/);});
