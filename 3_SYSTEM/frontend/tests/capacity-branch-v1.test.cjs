const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const src=fs.readFileSync(path.join(root,'app-no-reask-capacity-v1.js'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');test('BR-CAPACITY reads canonical EconomicInput capacity field',()=>{assert.match(src,/capacity_cost_rate_eur_hour/);assert.doesNotMatch(src,/\.capacity_rate_eur_hour/);assert.match(html,/app-no-reask-capacity-v1\.js/);});
